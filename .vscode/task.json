{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "make",
            "args": [
                "-f", "${workspaceFolder}/Makefile"  // Убедитесь, что путь к Makefile верный
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Компиляция проекта с использованием Makefile"
        }
    ]
}

